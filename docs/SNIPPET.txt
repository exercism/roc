factorial = |number| {
    match number {
        1 => 1
        n => n * factorial(n - 1)
    }
}

expect factorial(5) == 1 * 2 * 3 * 4 * 5

main! = |_args| {
    result = factorial(20)
    echo!("factorial 20 = ${result.to_str()}")
    Ok({})
}
