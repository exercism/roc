{%- import "generator_macros.j2" as macros with context -%}
{{ macros.canonical_ref() }}
{{ macros.header() }}

import {{ exercise | to_pascal }} exposing [{{ cases[0]["property"] | to_camel }}]

{% for case in cases -%}
# {{ case["description"] }}
expect
    result = {{ case["property"] | to_camel }} {{ case["input"]["start_verse"] | to_roc }} {{ case["input"]["end_verse"] | to_roc }}
    result == {{ case["expected"] | join('\n') | to_roc_multiline_string | indent(8) }}

{% endfor %}
