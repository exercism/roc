{%- import "generator_macros.j2" as macros with context -%}
{{ macros.canonical_ref() }}

{{ macros.header()}}

import Leap exposing [isLeapYear]

{% for case in cases -%}
# {{ case["description"] }}
expect
    result = (isLeapYear {{ case["input"]["year"] }})
    result == {{ case ["expected"] | to_roc }}

{% endfor %}
