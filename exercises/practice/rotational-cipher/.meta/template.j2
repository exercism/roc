{%- import "generator_macros.j2" as macros with context -%}
{{ macros.canonical_ref() }}
{{ macros.header() }}

import {{ exercise | to_pascal }} exposing [rotate]

{% for case in cases -%}
# {{ case["description"] }}
expect {{ case["property"] | to_camel }} {{ case["input"]["text"] | to_roc }} {{ case["input"]["shiftKey"] }} == {{ case["expected"] | to_roc }}

{% endfor %}
