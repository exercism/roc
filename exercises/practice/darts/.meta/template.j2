{%- import "generator_macros.j2" as macros with context -%}
{{ macros.canonical_ref() }}
{{ macros.header() }}

import Darts exposing [score]

{% for case in cases -%}
# {{ case["description"] }}
expect {
    result = {{ case["property"] | to_snake }}({{ case["input"]["x"] | to_roc_real }}, {{ case["input"]["y"] | to_roc_real }})
    result == {{ case["expected"] }}
}

{% endfor %}

{{ macros.footer() }}
