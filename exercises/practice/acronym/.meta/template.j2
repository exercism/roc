{%- import "generator_macros.j2" as macros with context -%}
{{ macros.canonical_ref() }}
{{ macros.header() }}

import {{ exercise | to_pascal }}

{% for case in cases -%}
# {{ case["description"] }}
expect {
    result = {{ exercise | to_pascal }}.{{ case["property"] | to_snake }}({{ case["input"]["phrase"] | to_roc }})
    result == {{ case["expected"] | to_roc }}
}

{% endfor %}
