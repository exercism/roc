{%- import "generator_macros.j2" as macros with context -%}
{{ macros.canonical_ref() }}
{{ macros.header() }}

import {{ exercise | to_pascal }} exposing [{{ cases[0]["property"] | to_snake }}]

{% for case in cases -%}
# {{ case["description"] }}
expect {
    result = {{ case["property"] | to_snake }}({{ case["input"]["colors"][0] | to_pascal }}, {{ case["input"]["colors"][1] | to_pascal }}, {{ case["input"]["colors"][2] | to_pascal }})
    result == {{ case["expected"]["unit"] | to_pascal }}({{ case["expected"]["value"] | to_roc }})
}

{% endfor %}

{{ macros.footer() }}
